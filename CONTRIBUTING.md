# Contributing

Contributions are welcome. Please keep edits in `sources/Traf-Regular.ufo` and make sure the project still builds with `make build`. Before proposing changes, run `make test` when FontBakery is available.

Design changes should preserve Traf's geometric construction, rounded rhythm, and the distinctive forms used by the core `Traf` lettering.
