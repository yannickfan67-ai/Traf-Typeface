# Source notes

The source of truth is `Traf-Regular.ufo`. The font began as lettering derived from generated visual references, then the selected glyphs were vectorized, normalized, expanded, and progressively hand/algorithmically refined into a font source. New work should be made in the UFO and rebuilt with `fontmake`; do not patch release TTFs without synchronizing the source.
